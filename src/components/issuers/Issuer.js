// Export a function that accepts a fund id, Modal, setModal, and prints that fund's issuer details

// Get the targeted issuer and save it to state

// Handle adding issuer to favorites
    // Send POST to issuer resource
    // Update state that closes modal

// Print the issuer details
    // Issuer's funds
    // Issuer image
    // Issuer's name
    // Issuers country
    // An add to favorites button
        // Check to see if the issuer is on the current user's favorite's list
            // If it is, change the button to "unfavorite"