// Export a function that accepts refresh and setRefresh states, then prints a list of the current user's fave issuers to the DOM

// Fetch the current user's favorite issuers and save to state

// Handle removing an issuer from the list
    // Send DELETE to fave url
    // setRefresh

// Render a list of those issuers to the DOM with a "remove" button for each